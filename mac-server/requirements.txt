# DEPRECATED — do not `pip install -r requirements.txt`.
#
# The worker uses two isolated Python 3.11 venvs with EXACT pinned versions.
# Install from the frozen snapshots instead (see mac-server/MIGRATION.md):
#
#   venv      (worker: FastAPI, torch/Demucs, librosa, BTC)
#     ./venv/bin/pip install -r requirements-worker.txt
#
#   venv-sep  (audio-separator Roformer + basic-pitch tabs)
#     ./venv-sep/bin/pip install -r requirements-separator.txt
#
# Regenerate after an intentional upgrade:
#   ./venv/bin/python     -m pip freeze > requirements-worker.txt
#   ./venv-sep/bin/python -m pip freeze > requirements-separator.txt
